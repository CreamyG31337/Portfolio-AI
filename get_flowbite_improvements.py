# Scan for flowbite usage that is bypassing APIs.
# The `ui.ts` / `ai_assistant.ts` / `jobs.ts` / `ticker_details.ts` use manual .hidden toggles instead of flowbite.
# The `trade_entry.ts` and `funds.ts` click on modal trigger elements instead of `new Modal`.
