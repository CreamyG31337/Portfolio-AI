{
  "name": "web-dashboard",
  "version": "1.0.0",
  "description": "TypeScript build configuration for web dashboard",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "tsc --pretty --listFiles --diagnostics",
    "watch": "tsc --watch",
    "clean": "rm -rf static/js/*.js static/js/*.js.map",
    "test": "vitest run"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.1",
    "flowbite": "^2.5.2",
    "jsdom": "^24.0.0",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "private": true
}