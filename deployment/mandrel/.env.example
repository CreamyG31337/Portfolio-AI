# ============================================================
# Mandrel MCP Server Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# For Woodpecker CI/CD, add these as secrets:
#   - mandrel_db_password
# ============================================================

# Database Configuration
# PostgreSQL is running in Docker container (postgres-17.5)
# Container is on PROD network, accessible via Docker network
MANDREL_DB_USER=postgres
MANDREL_DB_NAME=mandrel_dev
MANDREL_DB_PASSWORD=your_postgres_password_here
MANDREL_DB_PORT=5432

# ============================================================
# WOODPECKER CI/CD INTEGRATION
# ============================================================
# Add these secrets in Woodpecker:
#
#   mandrel_db_password - Your PostgreSQL password
#
# The docker-compose.yml uses these environment variables:
#   ${MANDREL_DB_PASSWORD} - Required, no default
#   ${MANDREL_DB_USER:-postgres} - Optional, defaults to postgres
#   ${MANDREL_DB_NAME:-mandrel_dev} - Optional, defaults to mandrel_dev
#   ${MANDREL_DB_PORT:-5432} - Optional, defaults to 5432
# ============================================================
